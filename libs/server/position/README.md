# position

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build position` to build the library.

## Running unit tests

Run `nx test position` to execute the unit tests via [Jest](https://jestjs.io).
